Libft is a custom C library that rerimplements many standard C functions like strlen, strcpy, strdup,
and also includes additional utility functions for string, memory and list manipulation.
It provides reusable components for other C projects.
Developed as part of the 42 curriculum.
